apiVersion: 1
providers:
  - name: koim-dashboards
    orgId: 1
    folder: koim
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
